{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Run repayment scenario",
      "cargo": {
        "args": ["build", "--bin", "keystone_xdtl"],
        "filter": {
          "name": "keystone_xdtl",
          "kind": "bin"
        }
      },
      "args": ["repayment"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
